import 'package:flutter/material.dart';

class DestinationModel {
  const DestinationModel(this.index, this.title, this.icon, this.color);
  final int index;
  final String title;
  final IconData icon;
  final MaterialColor color;
}
